EXPO_PUBLIC_API_URL=http://localhost:4000/api

# Public Mapbox access token used to render the Walk map (native + web).
# Create one at https://account.mapbox.com/access-tokens/
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_public_token_here
